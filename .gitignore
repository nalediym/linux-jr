node_modules
dist
.vercel
.env
.env.local
.DS_Store
.omc
*.log

# Claude Code — commit agents + hooks, keep session/tooling state local
.claude/worktrees/
.claude/settings.local.json

# Knowledge base — entirely local. Raw sources are user-managed, the wiki is
# LLM-compiled and noisy on diff, and the manifest thrashes on every compile.
# Nothing under kb/ should ever land in git.
kb/
